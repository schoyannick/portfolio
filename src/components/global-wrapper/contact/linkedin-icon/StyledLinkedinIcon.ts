import styled from 'styled-components';

export const StyledLinkedinIconWrapper = styled.a`
    display: flex;
    flex-direction: column;
    align-items: center;
    text-decoration: none;
    color: inherit;
`;